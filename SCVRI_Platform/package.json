{
  "name": "scvri-platform",
  "version": "1.0.0",
  "description": "Supply Chain Visibility & Risk Intelligence Platform",
  "private": true,
  "scripts": {
    "dev": "npm run --prefix frontend dev",
    "build": "npm run --prefix frontend build",
    "start": "npm run --prefix frontend start",
    "lint": "npm run --prefix frontend lint",
    "typecheck": "npm run --prefix frontend typecheck",
    "install:frontend": "npm install --prefix frontend",
    "prepare": "pre-commit install || true"
  },
  "engines": {
    "node": ">=18"
  }
}
